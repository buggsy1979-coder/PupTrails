<Window x:Class="PupTrailsV3.Views.AddMoneyOwedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Money Owed" Height="620" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource DarkBackground}">
    <Border Padding="25">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel VerticalAlignment="Top">
                <!-- Title -->
                <TextBlock Text="Record Money Owed" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentOrange}" Margin="0,0,0,20"/>

            <!-- Date -->
            <TextBlock Text="Date (YYYY-MM-DD)" FontWeight="Bold" Margin="0,0,0,6" Foreground="{StaticResource PrimaryText}" FontSize="13"/>
            <TextBox Name="DateTextBox" Margin="0,0,0,15" Padding="12" Height="48" FontSize="13"/>

            <!-- Debtor -->
            <TextBlock Text="Debtor Name" FontWeight="Bold" Margin="0,0,0,6" Foreground="{StaticResource PrimaryText}" FontSize="13"/>
            <TextBox Name="DebtorBox" Margin="0,0,0,15" Padding="12" Height="48" FontSize="13"/>

            <!-- Reason -->
            <TextBlock Text="Reason/Description" FontWeight="Bold" Margin="0,0,0,6" Foreground="{StaticResource PrimaryText}" FontSize="13"/>
            <TextBox Name="ReasonBox" Margin="0,0,0,15" Padding="12" Height="48" FontSize="13"/>

            <!-- Amount Owed -->
            <TextBlock Text="Amount Owed ($)" FontWeight="Bold" Margin="0,0,0,6" Foreground="{StaticResource PrimaryText}" FontSize="13"/>
            <TextBox Name="AmountOwedBox" Margin="0,0,0,15" Padding="12" Height="48" FontSize="13" TextChanged="AmountChanged"/>

            <!-- Amount Paid -->
            <TextBlock Text="Amount Paid ($)" FontWeight="Bold" Margin="0,0,0,6" Foreground="{StaticResource PrimaryText}" FontSize="13"/>
            <TextBox Name="AmountPaidBox" Margin="0,0,0,15" Padding="12" Height="48" FontSize="13" TextChanged="AmountChanged"/>

            <!-- Date Paid -->
            <TextBlock Text="Date Paid (YYYY-MM-DD)" FontWeight="Bold" Margin="0,0,0,6" Foreground="{StaticResource PrimaryText}" FontSize="13"/>
            <TextBox Name="DatePaidTextBox" Margin="0,0,0,15" Padding="12" Height="48" FontSize="13"/>

            <!-- Total Owed (Read-only, calculated) -->
            <TextBlock Text="Total Still Owed ($)" FontWeight="Bold" Margin="0,0,0,6" Foreground="{StaticResource PrimaryText}" FontSize="13"/>
            <TextBox Name="TotalOwedBox" Margin="0,0,0,15" Padding="12" Height="48" FontSize="13" IsReadOnly="True" Background="#1A1A1A" Foreground="{StaticResource AccentRed}" FontWeight="Bold"/>

            <!-- Notes -->
            <TextBlock Text="Notes" FontWeight="Bold" Margin="0,0,0,6" Foreground="{StaticResource PrimaryText}" FontSize="13"/>
            <TextBox Name="NotesBox" Margin="0,0,0,20" Padding="10" Height="80" TextWrapping="Wrap" FontSize="13" VerticalScrollBarVisibility="Auto"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Reset" Click="Reset_Click" Width="100" Height="40" Margin="0,0,10,0" Background="{StaticResource AccentOrange}" Cursor="Hand" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                <Button Content="Delete" Click="Delete_Click" Width="100" Height="40" Margin="0,0,10,0" Background="{StaticResource AccentRed}" Cursor="Hand" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                <Button Content="Cancel" Click="Cancel_Click" Width="100" Height="40" Margin="0,0,10,0" Background="#555555" Cursor="Hand" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                <Button Content="Save" Click="Save_Click" Width="100" Height="40" Background="{StaticResource AccentGreen}" Foreground="White" Cursor="Hand" FontWeight="Bold" FontSize="13"/>
            </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>
