<Window x:Class="PupTrailsV3.Views.ViewGroupDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Group Details" Height="700" Width="1000" WindowStartupLocation="CenterOwner" ResizeMode="CanResize" Background="{StaticResource DarkBackground}">
    <Grid Background="{StaticResource DarkBackground}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="GroupNameTitle" Text="Group Details" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentBlue}" Margin="0,0,0,10"/>
            <Border Background="{StaticResource DarkCardBackground}" Padding="15" CornerRadius="6" BorderThickness="1" BorderBrush="{StaticResource DarkBorder}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Total Members:" FontWeight="Bold" Foreground="{StaticResource PrimaryText}" FontSize="14" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TotalMembersText" Grid.Column="1" Text="0" Foreground="{StaticResource AccentGreen}" FontSize="14" FontWeight="Bold"/>
                    <TextBlock Grid.Column="2" Text="Date Created:" FontWeight="Bold" Foreground="{StaticResource PrimaryText}" FontSize="14" Margin="20,0,10,0"/>
                    <TextBlock x:Name="DateCreatedText" Grid.Column="3" Text="N/A" Foreground="{StaticResource SecondaryText}" FontSize="14"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Group Image Section -->
        <Border Grid.Row="1" Background="{StaticResource DarkCardBackground}" Padding="15" CornerRadius="6" Margin="0,0,0,15" BorderThickness="1" BorderBrush="{StaticResource DarkBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Image Display -->
                <Border Grid.Column="0" Width="200" Height="150" BorderBrush="{StaticResource DarkBorder}" BorderThickness="2" CornerRadius="6" Margin="0,0,20,0" Background="#2A2A2A">
                    <Grid>
                        <TextBlock x:Name="NoImageText" Text="No Group Photo" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource SecondaryText}" FontSize="12" FontStyle="Italic"/>
                        <Image x:Name="GroupImageDisplay" Stretch="UniformToFill" Visibility="Collapsed"/>
                    </Grid>
                </Border>
                
                <!-- Image Controls -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="📷 Group Photo" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentBlue}" Margin="0,0,0,10"/>
                    <TextBlock Text="Upload a photo of this puppy group to help identify them." Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,0,0,15" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="📁 Upload Image" Click="UploadImage_Click" Background="{StaticResource AccentGreen}" Foreground="White" Padding="15,10" FontSize="12" FontWeight="SemiBold" Cursor="Hand" Margin="0,0,10,0"/>
                        <Button x:Name="RemoveImageButton" Content="🗑️ Remove Image" Click="RemoveImage_Click" Background="{StaticResource AccentRed}" Foreground="White" Padding="15,10" FontSize="12" FontWeight="SemiBold" Cursor="Hand" IsEnabled="False"/>
                    </StackPanel>
                    <TextBlock x:Name="ImagePathText" Text="" Foreground="{StaticResource SecondaryText}" FontSize="10" Margin="0,10,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Filter/Search -->
        <Border Grid.Row="2" Background="{StaticResource DarkCardBackground}" Padding="10" CornerRadius="6" Margin="0,0,0,15" BorderThickness="1" BorderBrush="{StaticResource DarkBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🔍 Search:" VerticalAlignment="Center" Foreground="{StaticResource PrimaryText}" FontSize="13" FontWeight="SemiBold" Margin="0,0,10,0"/>
                <TextBox x:Name="SearchBox" Grid.Column="1" Padding="12" MinHeight="48" FontSize="13" TextChanged="SearchBox_TextChanged"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="10,0,0,0">
                    <Button Content="+ Add New Animal" Click="AddNewAnimal_Click" Background="{StaticResource AccentGreen}" Foreground="White" Padding="15,10" FontSize="13" FontWeight="SemiBold" Cursor="Hand"/>
                    <Button Content="📎 Add Existing" Click="AddExisting_Click" Background="{StaticResource AccentBlue}" Foreground="White" Margin="10,0,0,0" Padding="15,10" FontSize="13" FontWeight="SemiBold" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Members DataGrid -->
        <Border Grid.Row="3" Background="{StaticResource DarkCardBackground}" CornerRadius="6" BorderThickness="1" BorderBrush="{StaticResource DarkBorder}">
            <DataGrid x:Name="MembersDataGrid" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="{StaticResource DarkCardBackground}"
                      Foreground="{StaticResource PrimaryText}"
                      RowBackground="{StaticResource DarkBackground}"
                      AlternatingRowBackground="{StaticResource DarkCardBackground}"
                      BorderThickness="0"
                      FontSize="12"
                      MouseDoubleClick="MembersDataGrid_DoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" MinWidth="120"/>
                    <DataGridTextColumn Header="Breed" Binding="{Binding Breed}" Width="*" MinWidth="120"/>
                    <DataGridTextColumn Header="Sex" Binding="{Binding Sex}" Width="80"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Collar Color" Binding="{Binding CollarColor}" Width="130"/>
                    <DataGridTextColumn Header="Intake Date" Binding="{Binding IntakeDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                    <DataGridTextColumn Header="Date of Birth" Binding="{Binding DOB, StringFormat=yyyy-MM-dd}" Width="100"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Border>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="📄 Export to Text File" Click="ExportToFile_Click" Width="180" Height="40" Background="{StaticResource AccentBlue}" Foreground="White" FontSize="13" FontWeight="SemiBold" Cursor="Hand" Margin="0,0,10,0"/>
            <Button Content="Close" Click="Close_Click" Width="120" Height="40" Background="#555555" Foreground="White" FontSize="13" FontWeight="SemiBold" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
